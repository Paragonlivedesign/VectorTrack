# VectorTrack 0.5.0 release builds

| File | Purpose |
|------|---------|
| `VectorTrack.exe` | Portable app (with `_internal/` dependencies) |
| `VectorTrack-0.5.0-Setup.exe` | Windows installer (Inno Setup) |

Rebuild from the parent folder:

```powershell
.\build.ps1 -WithInstaller
```
